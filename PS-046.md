# PS-046 — Smart Hotel Management System
## Project Documentation

---

> **Project ID**: PS-046  
> **Project Name**: Smart AI Hotel Management System  
> **Location**: `c:\Users\chitt\smart-hotel-system`  
> **ServiceNow Instance**: `https://dev189725.service-now.com`  
> **App Scope**: `x_1939650_smart_0`  
> **Status**: ✅ Active Development  
> **Last Updated**: May 12, 2026

---

## 1. Project Overview

The **Smart Hotel Management System** is a full-stack, AI-powered hotel management platform built for **Novotel Visakhapatnam Varun Beach**. It integrates a premium React front-end with a ServiceNow backend to deliver a complete digital hospitality experience for guests, staff, receptionists, managers, and admins.

### Key Objectives
- Provide guests with a self-service portal for bookings, food orders, and service requests
- Enable staff with a real-time task management interface
- Give managers live analytics and occupancy dashboards
- Synchronize all data bi-directionally with ServiceNow REST APIs
- Achieve 95+ Lighthouse scores across Performance, Accessibility, SEO, and Best Practices

---

## 2. Technology Stack

| Layer | Technology | Version |
|---|---|---|
| UI Framework | React | 19.x |
| Build Tool | Vite | 8.x |
| Styling | Tailwind CSS | 4.x |
| Routing | React Router DOM | 7.x |
| State Management | Zustand | 5.x |
| Animations | Framer Motion | 12.x |
| Charts | Recharts | 3.x |
| Icons | Lucide React | 1.x |
| 3D Graphics | Three.js / R3F | 0.184 |
| HTTP Client | Axios | 1.x |
| Auth Backend | Supabase | 2.x |
| Notifications | EmailJS Browser | 4.x |
| Data Backend | ServiceNow REST API | — |
| Testing | Vitest + Testing Library | 4.x |

---

## 3. Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   React Frontend (Vite)                     │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  Guest   │  │  Staff   │  │ Manager  │  │  Admin   │   │
│  │  Portal  │  │  Portal  │  │ Analytics│  │  Portal  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Zustand State Store                     │   │
│  │  authStore (user, role, session)                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │               lib/servicenow.js                      │   │
│  │      Axios instance → ServiceNow Table API           │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              lib/notifications.js                    │   │
│  │  notify() → Toast + Desktop + EmailJS                │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                          │
                    REST API (CORS)
                          │
┌─────────────────────────────────────────────────────────────┐
│              ServiceNow (dev189725)                         │
│                                                             │
│  Tables: bookings, rooms, food_orders, guest_incidents,     │
│          notifications, payments, feedback, staff_tasks,    │
│          chat_logs, sla_metrics, hotel_user                 │
│                                                             │
│  Business Rules (7) | Flows (3) | Scheduled Jobs (3)       │
└─────────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────────┐
│                     Supabase                                │
│  Auth: User registration, email verification, sessions      │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. Project File Structure

```
smart-hotel-system/
├── index.html                    # SEO-optimised entry point
├── vite.config.js                # Build config with code splitting
├── package.json                  # Dependencies
├── .env                          # Secret credentials (never commit)
├── .env.example                  # Template for new developers
├── public/
│   ├── robots.txt                # SEO crawler rules
│   └── favicon.svg
├── src/
│   ├── main.jsx                  # App entry
│   ├── App.jsx                   # Router configuration
│   ├── index.css                 # Global styles + Tailwind
│   ├── lib/
│   │   ├── servicenow.js         # Axios + ServiceNow REST client
│   │   └── notifications.js      # Toast + Desktop + Email notify()
│   ├── store/
│   │   └── authStore.js          # Zustand auth (Supabase + Admin)
│   ├── components/
│   │   └── Layout.jsx            # Sidebar navigation + weather widget
│   └── pages/
│       ├── Login.jsx             # Unified login page
│       ├── Dashboard.jsx         # Role-based router
│       ├── FoodAndDrinks.jsx     # Food ordering page
│       ├── NearbyPlaces.jsx      # Interactive Google Maps page
│       ├── StaffManagement.jsx   # Admin staff directory
│       └── dashboards/
│           ├── CustomerDashboard.jsx   # Full guest portal
│           ├── AdminDashboard.jsx      # Admin home
│           ├── StaffDashboard.jsx      # Staff task view
│           ├── ManagerDashboard.jsx    # Manager analytics
│           └── ReceptionistDashboard.jsx
```

---

## 5. Environment Variables

**File**: `.env` — never commit to Git

```env
# ServiceNow
VITE_SN_INSTANCE=https://dev189725.service-now.com
VITE_SN_USERNAME=admin
VITE_SN_PASSWORD=<password>

# Supabase Auth
VITE_SUPABASE_URL=https://otnubbxpwfmwtyncuujx.supabase.co
VITE_SUPABASE_ANON_KEY=<anon_key>

# Admin Login (verified against env, never hardcoded in source)
VITE_ADMIN_EMAIL=admin@hotel.com
VITE_ADMIN_PASSWORD=<password>

# EmailJS — Real email notifications (optional)
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_BOOKING=your_booking_template_id
VITE_EMAILJS_TEMPLATE_SERVICE=your_service_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key
```

---

## 6. User Roles & Access

| Role | Login Method | Portal Features |
|---|---|---|
| **Customer** | Supabase Email Auth | Book rooms, food orders, service requests, notifications, payments, nearby places, feedback |
| **Staff** | Supabase Email Auth | Task management, assigned incidents |
| **Receptionist** | Supabase Email Auth | Check-in/out, guest list |
| **Manager** | Supabase Email Auth | Analytics, occupancy, revenue charts |
| **Admin** | `.env` credentials | All portals + Staff Directory (sys_user table) |

---

## 7. Guest Portal Pages

### Home Hub (`/`)
- Navigation card grid linking to all sections
- Live status: recent bookings, latest food order, latest service request
- Weather widget (Open-Meteo API, Vizag) + live IST clock

### Book Room (`/book-room`)
- Lists available rooms from ServiceNow room table
- Booking modal: room, check-in/out, guests, phone, special requests
- Auto-calculates price, posts to bookings table
- On success: `notify()` fires (in-app toast + desktop popup)

### Food & Drinks (`/food-drinks`)
- Grid/list view of menu with category filter
- Posts orders to food_orders table

### Room Services (`/services`)
- Live service incidents with status and priority
- Rapid Service modal: type, priority, description
- Posts to guest_incidents table, fires `notify()` on success

### Nearby Places (`/nearby`)
- Interactive Google Maps (dark mode filter)
- 5 Vizag attractions with distance, travel time, rating
- Click card → map pans to location

### Notifications (`/notifications`)
- Permission banner: Enabled / Blocked / Not Set
- Enable Notifications button → browser prompt
- 🔔 Send Test button
- Live Activity Timeline (bookings + food orders + incidents)

### Payments (`/payments`)
- Outstanding balance + payment history

### Feedback (`/feedback`)
- Star rating (1-5) + written review → posts to feedback table

### Settings (`/settings`)
- Profile: name, email, notification preferences

---

## 8. Notification System

Three-layer architecture (most reliable first):

```
notify(title, body, type)
    │
    ├── Layer 1: react-hot-toast — ALWAYS WORKS, no permissions
    │           Dark-styled toast, 6 second duration
    │
    ├── Layer 2: In-app event emitter → Activity Timeline
    │           Shows in Notifications page feed
    │
    └── Layer 3: Browser Notification API — if OS permits
                Works on localhost + HTTPS
                May be blocked by Windows Focus Assist
```

To enable desktop popups:
1. Go to **Notifications** page in app
2. Click **"Enable Notifications"** → Allow in browser dialog
3. Disable Windows **Focus Assist**: Settings → System → Notifications → Focus Assist → Off

---

## 9. ServiceNow Tables

| Table | Purpose |
|---|---|
| `x_1939650_smart_0_bookings` | Room reservations |
| `x_1939650_smart_0_room` | Room inventory & availability |
| `x_1939650_smart_0_food_orders` | Restaurant / room service orders |
| `x_1939650_smart_0_guest_incidents` | Service requests & complaints |
| `x_1939650_smart_0_notifications` | Email/push notification queue |
| `x_1939650_smart_0_payments` | Billing & payment records |
| `x_1939650_smart_0_feedback` | Guest reviews and ratings |
| `x_1939650_smart_0_staff_tasks` | Housekeeping/delivery tasks |
| `x_1939650_smart_0_sla_metrics` | Response time tracking |
| `x_1939650_smart_0_chat_logs` | AI chatbot conversations |
| `x_1939650_smart_0_hotel_user` | Custom user registry |
| `sys_user` | Staff Directory (Admin view) |

---

## 10. ServiceNow Business Rules

| # | Name | Table | When | Action |
|---|---|---|---|---|
| 1 | Auto-Calculate Booking Price | bookings | Before Insert/Update | Total = room_rate × nights |
| 2 | Generate Booking ID | bookings | Before Insert | `BK-YYYY-XXXXX` |
| 3 | Generate Order ID | food_orders | Before Insert | `ORD-YYYY-XXXXX` |
| 4 | Generate Incident ID | guest_incidents | Before Insert | `INC-YYYY-XXXXX` |
| 5 | Auto-Assign Urgent Incidents | guest_incidents | After Insert | Assigns to Manager role |
| 6 | Generate Task ID | staff_tasks | Before Insert | `TSK-YYYY-XXXXX` |
| 7 | Generate Chat ID | chat_logs | Before Insert | `CHT-YYYY-XXXXX` |

---

## 11. ServiceNow Flows

| Flow | Trigger | Actions |
|---|---|---|
| Booking Confirmation | Booking created (status=confirmed) | Set room → Occupied; Create notification |
| Incident Auto-Assignment | Incident created | Set status → Assigned; Create SLA record |
| Food Order Processing | Food order created | Create Staff Task for delivery |

---

## 12. ServiceNow Scheduled Jobs

| Job | Schedule | Action |
|---|---|---|
| Monitor SLA Breaches | Every 15 minutes | Checks resolution time, sets → Breached |
| Auto-Close Resolved Incidents | Daily | Closes resolved incidents >24h old |
| Process Pending Notifications | Every 5 minutes | Marks pending notifications as Sent |

---

## 13. Lighthouse Optimisations

| Fix | Category |
|---|---|
| `<title>` + `<meta description>` | SEO |
| `lang="en"` on html element | Accessibility |
| `preconnect` for Fonts/Maps/Unsplash | Performance |
| `aria-label` on all icon buttons | Accessibility |
| `htmlFor`/`id` on all form inputs | Accessibility |
| `aria-current="page"` on active nav | Accessibility |
| `:focus-visible` CSS ring | Accessibility |
| `loading="lazy"` + `width`/`height` on images | Performance |
| Code splitting (vendor/motion/charts/store) | Performance |
| `robots.txt` | SEO |
| `theme-color` meta tag | Best Practices |

> Always test Lighthouse on `npm run preview` (port 4173), not `npm run dev`

---

## 14. Run Commands

```bash
npm install          # Install dependencies
npm run dev          # Development server → localhost:5173
npm run build        # Production build → dist/
npm run preview      # Preview build → localhost:4173 (use for Lighthouse)
npm run test         # Run Vitest unit tests
```

---

## 15. Known Field Corrections (ServiceNow)

| Location | Old Field | Correct Field |
|---|---|---|
| Chat Logs | `u_user_name` | `user_name` |
| Room table room_number | Integer | String |
| SLA Metrics | `u_current_time` | `current_time` |
| Business Rule 7 | `u_user_name` ref | `user_name` ref |
| Scheduled Job 1 | `sla.u_current_time` | `sla.current_time` |

---

## 16. Developer Info

| Field | Value |
|---|---|
| Developer | chittemreddypranay |
| Email | chittemreddypranay@gmail.com |
| ServiceNow Instance | dev189725.service-now.com |
| Supabase Project | otnubbxpwfmwtyncuujx |

---

*Document ID: PS-046 | Smart Hotel Management System | May 12, 2026*
